[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-lessons/deepagents/m2/m2.1-the-deep-agent-environment.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>
<script src="https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.min.js"></script>
<script>mermaid.initialize({ startOnLoad: true, theme: 'base', themeVariables: { background: '#F2FAFF', primaryColor: '#E5F4FF', primaryBorderColor: '#006DDD', primaryTextColor: '#030710', lineColor: '#2F4B68' } });</script>

# The Deep Agent Environment

<style>
.lt-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin: 28px 0 0;
  border-bottom: 2px solid #CCE9FF;
}
.lt-group { display: flex; gap: 3px; }
.lt-env  { --c: #006DDD; }
.lt-quiz { --c: #7C3AED; }
.lt-tab {
  font: 500 14px 'IBM Plex Mono', monospace;
  padding: 9px 14px;
  border: none;
  background: transparent;
  color: #40668D;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  border-radius: 6px 6px 0 0;
  transition: background .15s, color .15s, border-color .15s;
  white-space: nowrap;
}
.lt-tab:hover { background: #F2FAFF; color: #030710; }
.lt-tab.active {
  color: var(--c);
  border-bottom-color: var(--c);
  background: #fff;
}
.lt-panel { display: none; padding-top: 24px; }
.lt-panel.active { display: block; }
@media (max-width: 600px) {
  .lt-bar { flex-wrap: nowrap; overflow-x: auto; gap: 12px; }
  .lt-tab { padding: 8px 10px; font-size: 13px; }
}
</style>

<div class="lt-bar" role="tablist" aria-label="Lesson sections">
  <div class="lt-group lt-env">
    <button class="lt-tab active" data-p="env" role="tab" aria-selected="true">Environment</button>
  </div>
  <div class="lt-group lt-quiz">
    <button class="lt-tab" data-p="quiz" role="tab" aria-selected="false">Quiz</button>
  </div>
</div>

<div class="lt-panel active" id="p-env" markdown="1" role="tabpanel">

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/E7YE2JrxVUO" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

A deep agent does not run in the abstract. It runs inside an **environment**: the place where it can store files, run commands, and optionally execute code inside the agent loop.

Every Deep Agent environment has:

- a **filesystem**: always present, for reading and writing files
- a **shell**: optional, exposed through the `execute` tool when the environment supports command execution
- an **interpreter**: optional, a separate capability for running code inside the agent loop

We will look at those three capabilities first, then define the **backend** layer that implements the filesystem and optional shell. The interpreter is separate from the backend: it is added as its own tool/capability in the agent loop.

---

## File System

<div style="text-align: center;">
<svg viewBox="0 0 720 190" width="100%" role="img" aria-label="LLM calls filesystem tools (read_file, write_file, edit_file, ls, grep, glob) which pass through a Backend to storage." xmlns="http://www.w3.org/2000/svg" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">
  <rect width="720" height="190" fill="#030710" rx="8"/>

  <!-- LLM -->
  <rect x="24" y="65" width="82" height="60" rx="8" fill="#161F34" stroke="#7FC8FF" stroke-width="1.5"/>
  <text x="65" y="100" text-anchor="middle" font-size="16" font-weight="600" fill="#F2FAFF">LLM</text>

  <!-- Arrow LLM → Backend with tool labels above -->
  <line x1="106" y1="95" x2="375" y2="95" stroke="#40668D" stroke-width="1.5"/>
  <polygon points="375,88 387,95 375,102" fill="#40668D"/>
  <text x="244" y="58" text-anchor="middle" font-size="11" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">read_file · write_file · edit_file</text>
  <text x="244" y="71" text-anchor="middle" font-size="11" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">ls · grep · glob</text>

  <!-- Backend box -->
  <rect x="387" y="55" width="126" height="80" rx="8" fill="#0D1322" stroke="#7FC8FF" stroke-width="1.5"/>
  <text x="450" y="100" text-anchor="middle" font-size="16" font-weight="600" fill="#7FC8FF">Backend</text>

  <!-- Arrow Backend → disk -->
  <line x1="513" y1="95" x2="546" y2="95" stroke="#40668D" stroke-width="1.5"/>
  <polygon points="546,88 558,95 546,102" fill="#40668D"/>

  <!-- Disk cylinder -->
  <rect x="558" y="63" width="114" height="64" fill="#161F34" stroke="none"/>
  <line x1="558" y1="63" x2="558" y2="127" stroke="#7FC8FF" stroke-width="1.5"/>
  <line x1="672" y1="63" x2="672" y2="127" stroke="#7FC8FF" stroke-width="1.5"/>
  <line x1="559" y1="80" x2="671" y2="80" stroke="#40668D" stroke-width="1"/>
  <line x1="559" y1="96" x2="671" y2="96" stroke="#40668D" stroke-width="1"/>
  <line x1="559" y1="112" x2="671" y2="112" stroke="#40668D" stroke-width="1"/>
  <ellipse cx="615" cy="127" rx="57" ry="14" fill="#0D1322" stroke="#7FC8FF" stroke-width="1.5"/>
  <ellipse cx="615" cy="63" rx="57" ry="14" fill="#1a2744" stroke="#7FC8FF" stroke-width="1.5"/>
  <circle cx="615" cy="63" r="5" fill="#7FC8FF"/>
</svg>

<p><em>The LLM calls standard filesystem tools; the backend determines where the data actually lives.</em></p>
</div>

A filesystem gives the agent a place to work. It can use files as a scratchpad for notes, intermediate results, plans, and generated artifacts.

Deep Agents always provide filesystem tools such as `ls`, `read_file`, `write_file`, `edit_file`, `glob`, and `grep`. The agent sees the same tool surface even if the backend implementation changes.

That backend detail matters when you care where files live, but this lesson only needs the core idea: the filesystem is always part of the environment.

---

## Shell

<div style="text-align: center;">
<svg viewBox="0 0 720 190" width="100%" role="img" aria-label="LLM calls filesystem tools plus execute, which go to a Sandbox, an isolated environment." xmlns="http://www.w3.org/2000/svg" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">
  <rect width="720" height="190" fill="#030710" rx="8"/>

  <!-- LLM -->
  <rect x="24" y="65" width="82" height="60" rx="8" fill="#161F34" stroke="#7FC8FF" stroke-width="1.5"/>
  <text x="65" y="100" text-anchor="middle" font-size="16" font-weight="600" fill="#F2FAFF">LLM</text>

  <!-- Arrow LLM → Sandbox with tool labels above -->
  <line x1="106" y1="95" x2="378" y2="95" stroke="#40668D" stroke-width="1.5"/>
  <polygon points="378,88 390,95 378,102" fill="#40668D"/>
  <text x="246" y="58" text-anchor="middle" font-size="11" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">read_file · write_file · edit_file</text>
  <text x="246" y="71" text-anchor="middle" font-size="11" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">ls · grep · glob · <tspan font-weight="700" fill="#CCE9FF">execute</tspan></text>
  <text x="246" y="122" text-anchor="middle" font-size="11" fill="#40668D" font-family="'IBM Plex Mono', ui-monospace, monospace">execute("ls -la /workspace")</text>

  <!-- Sandbox as terminal window -->
  <rect x="390" y="38" width="296" height="114" rx="8" fill="#0D1322" stroke="#7FC8FF" stroke-width="1.5"/>
  <!-- Title bar fill -->
  <rect x="391" y="39" width="294" height="24" rx="6" fill="#1a2744"/>
  <rect x="391" y="52" width="294" height="11" fill="#1a2744"/>
  <!-- Traffic lights -->
  <circle cx="408" cy="51" r="4" fill="#FF5F57" opacity="0.85"/>
  <circle cx="422" cy="51" r="4" fill="#FFBD2E" opacity="0.85"/>
  <circle cx="436" cy="51" r="4" fill="#28CA41" opacity="0.85"/>
  <!-- Title -->
  <text x="538" y="56" text-anchor="middle" font-size="12" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">local shell / sandbox</text>
  <!-- Prompt -->
  <text x="410" y="104" font-size="14" fill="#7FC8FF" font-family="'IBM Plex Mono', ui-monospace, monospace">$ _</text>
</svg>

<p><em>A shell-capable backend adds <code>execute</code> to the filesystem interface.</em></p>
</div>

A shell lets the agent run commands through the `execute` tool: scripts, tests, package commands, and other process-level work. This is powerful, so the implementation matters.

Different backends can provide different implementations of `execute`. Two common families are:

**Sandbox providers** run commands in isolated environments such as containers, VMs, or remote sandboxes. This limits what code can affect compared with running directly on your machine, but safety still depends on sandbox configuration, network access, credentials, and provider isolation.

**Local shell** runs commands directly on your machine. It has no isolation overhead and can access local resources, but it should be used only when that local access is actually required.

The key distinction: shell execution, when available, is provided by the environment backend.

---

## The interpreter

<div style="text-align: center;">
<svg viewBox="0 0 720 322" width="100%" role="img" aria-label="Sequence diagram: Client sends a request to the LLM; the LLM generates a code block and sends it to the Interpreter; the Interpreter executes the code and returns the final result to the LLM; the LLM sends the final response back to the Client." xmlns="http://www.w3.org/2000/svg" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">
  <rect width="720" height="322" fill="#030710" rx="8"/>

  <!-- Participant labels -->
  <text x="90" y="24" text-anchor="middle" font-size="13" fill="#CCE9FF">Client</text>
  <text x="360" y="24" text-anchor="middle" font-size="13" fill="#CCE9FF">LLM</text>
  <text x="630" y="24" text-anchor="middle" font-size="13" fill="#CCE9FF">Interpreter</text>

  <!-- Client: person icon -->
  <circle cx="90" cy="56" r="22" fill="#161F34" stroke="#40668D" stroke-width="1.5"/>
  <circle cx="90" cy="48" r="8" fill="none" stroke="#7FC8FF" stroke-width="1.5"/>
  <path d="M73,72 Q73,62 90,62 Q107,62 107,72" fill="none" stroke="#7FC8FF" stroke-width="1.5" stroke-linecap="round"/>

  <!-- LLM: concentric rings -->
  <circle cx="360" cy="56" r="22" fill="#161F34" stroke="#7FC8FF" stroke-width="1.5"/>
  <circle cx="360" cy="56" r="12" fill="none" stroke="#7FC8FF" stroke-width="1" opacity="0.6"/>
  <circle cx="360" cy="56" r="5" fill="#7FC8FF"/>

  <!-- Interpreter: {} code symbol -->
  <circle cx="630" cy="56" r="22" fill="#161F34" stroke="#40668D" stroke-width="1.5"/>
  <text x="630" y="61" text-anchor="middle" font-size="15" font-weight="700" fill="#7FC8FF" font-family="'IBM Plex Mono', ui-monospace, monospace">{}</text>

  <!-- Lifelines -->
  <line x1="90" y1="78" x2="90" y2="314" stroke="#40668D" stroke-width="1" stroke-dasharray="4 3"/>
  <line x1="360" y1="78" x2="360" y2="314" stroke="#40668D" stroke-width="1" stroke-dasharray="4 3"/>
  <line x1="630" y1="78" x2="630" y2="314" stroke="#40668D" stroke-width="1" stroke-dasharray="4 3"/>

  <!-- Step 1: Client → LLM (REQUEST) -->
  <line x1="90" y1="108" x2="326" y2="108" stroke="#7FC8FF" stroke-width="1.5"/>
  <polygon points="326,101 338,108 326,115" fill="#7FC8FF"/>
  <circle cx="90" cy="108" r="10" fill="#006DDD"/>
  <text x="90" y="112" text-anchor="middle" font-size="12" font-weight="700" fill="#F2FAFF">1</text>
  <text x="213" y="94" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">REQUEST</text>

  <!-- Step 2: LLM → Interpreter (CODE BLOCK) -->
  <line x1="360" y1="144" x2="596" y2="144" stroke="#7FC8FF" stroke-width="1.5"/>
  <polygon points="596,137 608,144 596,151" fill="#7FC8FF"/>
  <circle cx="360" cy="144" r="10" fill="#006DDD"/>
  <text x="360" y="148" text-anchor="middle" font-size="12" font-weight="700" fill="#F2FAFF">2</text>
  <text x="490" y="130" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">CODE BLOCK</text>

  <!-- Code block -->
  <rect x="374" y="154" width="244" height="82" rx="4" fill="#0D1322" stroke="#40668D" stroke-width="1"/>
  <text x="386" y="174" font-size="12" fill="#7FC8FF" font-family="'IBM Plex Mono', ui-monospace, monospace">const results = search(query)</text>
  <text x="386" y="192" font-size="12" fill="#7FC8FF" font-family="'IBM Plex Mono', ui-monospace, monospace">const relevant = check(results)</text>
  <text x="386" y="210" font-size="12" fill="#7FC8FF" font-family="'IBM Plex Mono', ui-monospace, monospace">createTicket(relevant)</text>
  <text x="386" y="228" font-size="12" fill="#40668D" font-family="'IBM Plex Mono', ui-monospace, monospace">...</text>

  <!-- Step 3: EXECUTE (at Interpreter) -->
  <circle cx="630" cy="250" r="10" fill="#006DDD"/>
  <text x="630" y="254" text-anchor="middle" font-size="12" font-weight="700" fill="#F2FAFF">3</text>
  <text x="648" y="254" text-anchor="start" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">EVALUATE</text>

  <!-- Step 4: Interpreter → LLM (RETURN FINAL RESULT) -->
  <line x1="630" y1="274" x2="394" y2="274" stroke="#7FC8FF" stroke-width="1.5"/>
  <polygon points="394,267 382,274 394,281" fill="#7FC8FF"/>
  <circle cx="630" cy="274" r="10" fill="#006DDD"/>
  <text x="630" y="278" text-anchor="middle" font-size="12" font-weight="700" fill="#F2FAFF">4</text>
  <text x="495" y="260" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">RETURN FINAL RESULT</text>

  <!-- Step 5: LLM → Client (FINAL RESPONSE) -->
  <line x1="360" y1="298" x2="124" y2="298" stroke="#7FC8FF" stroke-width="1.5"/>
  <polygon points="124,291 112,298 124,305" fill="#7FC8FF"/>
  <circle cx="360" cy="298" r="10" fill="#006DDD"/>
  <text x="360" y="302" text-anchor="middle" font-size="12" font-weight="700" fill="#F2FAFF">5</text>
  <text x="225" y="284" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">FINAL RESPONSE</text>
</svg>

<p><em>The LLM writes code-like instructions; the interpreter runs them and returns the real result.<br>The snippet is pseudo-code.</em></p>
</div>

An interpreter is different from a shell. It does not run through the backend. It is a separate tool/capability inside the agent loop.

Use an interpreter when the agent needs code-like control flow without starting a full shell process. For example, instead of asking the model to call a tool 100 times, the interpreter can run a loop, collect intermediate results in variables, and return only the final summary to the model.

### Why use interpreters?

Most agent work alternates between model reasoning and tool calls. A model can fire several tool calls in one turn, but that batch is fixed the moment it is emitted. Nothing can loop, branch on a result, retry a failure, or feed one call's output into the next without another model turn, and every result returns to the model's context.

Interpreters give the agent a runtime for that work. A loop runs every iteration, intermediate values stay in variables, and only a compact result returns to the model.

---

## Backends

Now that you have seen the environment surface, we can name the implementation layer.

- The environment is what the agent sees: filesystem tools, optional `execute`, and optional interpreter.
- The backend is what implements the filesystem and optional shell behind that environment.

A backend answers questions like:

- where do files live?
- is `execute` available?
- if `execute` is available, where do commands run?

There is an abstract backend interface with multiple implementations. Some implementations only provide filesystem storage. Some also provide shell execution. Shell execution can be backed by a local shell or by different sandbox providers.

The key point is that the agent's tool surface can stay the same while the backend changes. The agent can still call `read_file` or `write_file`; the backend decides what those calls mean underneath.

The interpreter is not part of this backend layer. It is a separate capability added to the agent loop.

---

## Recap

- A deep agent runs inside an **environment**
- The filesystem is always present
- A shell is optional and appears as the `execute` tool when the backend supports command execution
- A **backend** implements the filesystem and, when available, shell execution
- The **interpreter** is optional and separate from the backend; it runs as its own tool/capability inside the agent loop

---

## References

**Documentation:**

- [Filesystem Backends](https://docs.langchain.com/oss/python/deepagents/backends)

</div>

<div class="lt-panel" id="p-quiz" role="tabpanel">

<h2>Check your understanding</h2>

<MCQ
    question="What is always present in a Deep Agent environment?"
    choices='["A filesystem", "A shell", "An interpreter", "A database connection"]'
    correctIndex={0}
    explanation="Every Deep Agent environment includes a filesystem. Shell access and interpreter support are optional capabilities."
/>

<MCQ
    question="What does a backend implement?"
    choices='["The model provider", "The system prompt", "The filesystem and, when available, shell execution", "The interpreter runtime only"]'
    correctIndex={2}
    explanation="The backend is the implementation behind the filesystem and optional shell execution. It decides where files live and where commands run."
/>

<MCQ
    question="Why might you choose different backends?"
    choices='["To change persistence, isolation, or locality tradeoffs", "To change the language model weights", "To remove the need for tools", "To make messages disappear from traces"]'
    correctIndex={0}
    explanation="Different backends make different tradeoffs: where files live, whether command execution is isolated, and whether work happens locally or remotely."
/>

<MCQ
    question="How is the interpreter different from filesystem tools and the execute tool?"
    choices='["It runs through a separate Interpreter backend", "It is only available in the Sandbox backend", "It calls the LLM directly instead of executing code", "It is a separate tool/capability inside the agent loop, not part of the backend"]'
    correctIndex={3}
    explanation="The interpreter is separate from the backend. Filesystem tools and execute are backend-backed; the interpreter is added as its own capability inside the agent loop."
/>

</div>

<script>
(function () {
  var tabs = document.querySelectorAll('.lt-tab');
  function show(p) {
    tabs.forEach(function (t) {
      var on = t.getAttribute('data-p') === p;
      t.classList.toggle('active', on);
      t.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    document.querySelectorAll('.lt-panel').forEach(function (panel) {
      panel.classList.toggle('active', panel.id === 'p-' + p);
    });
  }
  tabs.forEach(function (t) {
    t.addEventListener('click', function () { show(t.getAttribute('data-p')); });
  });
})();
</script>
