[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-lessons/deepagents/m1/m1.2-running-a-deep-agent.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

<div class="page-lang-switch" data-page-lang-switch>
  <button class="page-lang-btn" data-page-lang="python" aria-selected="false">Python</button>
  <button class="page-lang-btn" data-page-lang="typescript" aria-selected="false">TypeScript</button>
</div>

# Running a Deep Agent

## Your First Deep Agent

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/Qrr9oLMWEmo" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

<div data-lang="python" markdown="1">
The point of this lesson is to get one minimal deep agent running before we start customizing it. By the end, you should know the smallest useful call to `create_deep_agent(...)`, where the shared course model comes from, and what a successful agent invocation returns.


Open `python/m1/m1.2_scratch_agent.py`:

```python
from deepagents import create_deep_agent

from models import model

agent = create_deep_agent(model=model)

result = agent.invoke({"messages": [{"role": "user", "content": "What is an LLM?"}]})

print(result["messages"][-1].content)
```

</div>

<div data-lang="typescript" markdown="1">

The point of this lesson is to get one minimal deep agent running before we start customizing it. By the end, you should know the smallest useful call to `createDeepAgent(...)`, where the shared course model comes from, and what a successful agent invocation returns.

Open `typescript/m1/m1.2_scratch_agent.ts`:

```typescript
import { createDeepAgent } from "deepagents";

import { model } from "../models.js";

const agent = createDeepAgent({ model });

const result = await agent.invoke({
    messages: [{ role: "user", content: "What is an LLM?" }]
});

console.log(result.messages.at(-1)?.content);
```

</div>

This is the smallest version of the agent we will build on throughout the course: a model wrapped by the Deep Agents harness, invoked with a normal chat message, and read from the final returned message.

Run it:

<div data-lang="python" markdown="1">

```bash
cd python
uv run ./m1/m1.2_scratch_agent.py
```

</div>

<div data-lang="typescript" markdown="1">

```bash
cd typescript
pnpm tsx ./m1/m1.2_scratch_agent.ts
```

</div>

**Code output:**

<pre class="no-copy"><code class="language-plaintext">An LLM (Large Language Model) is a type of artificial intelligence system
trained on vast amounts of text data to understand and generate human language.

Key characteristics:
- Scale: Contains billions to hundreds of billions of parameters
- Training: Trained using deep learning on huge text corpora from the internet,
  books, and other sources
- Capability: Can perform a wide range of language tasks: answering questions,
  summarizing, translating, writing, coding, reasoning, and more
- Architecture: Built on transformer architecture, which uses attention
  mechanisms to understand relationships between words across long passages
- Generative: Produces text by predicting the next token one at a time,
  based on context

Examples: GPT-4, Claude, Gemini, LLaMA, Mistral
</code></pre>

<div data-lang="python">
<div class="callout">
<strong>Troubleshooting</strong>
<ul>
<li><strong><code>ModuleNotFoundError: No module named '...'</code></strong>: you ran the script with <code>python3</code> directly. Use <code>uv run</code> instead; it activates the project's virtual environment where all dependencies live.</li>
<li><strong><code>error: Failed to spawn</code></strong> / <strong><code>Project directory does not exist</code></strong>: you're in the wrong directory. Run the command from the <code>lca-deepagents</code> repo root.</li>
</ul>
</div>
</div>

<div data-lang="typescript">
<div class="callout">
<strong>Troubleshooting</strong>
<ul>
<li><strong><code>Cannot find module 'deepagents'</code></strong>: dependencies aren't installed. Run <code>pnpm install</code> from the <code>typescript</code> directory.</li>
<li><strong><code>ENOENT</code></strong> / <strong><code>no such file or directory</code></strong>: you're in the wrong directory. Run the command from the <code>lca-deepagents/typescript</code> directory.</li>
<li><strong><code>Cannot find module '../models.js'</code></strong>: relative imports need the <code>.js</code> extension even though the source file is <code>models.ts</code> — this is how TypeScript's ESM output resolves at runtime.</li>
<li><strong><code>Unknown file extension ".ts"</code></strong> / <strong><code>ERR_UNKNOWN_FILE_EXTENSION</code></strong>: you ran the file with plain <code>node</code> instead of <code>pnpm tsx</code>, which this course's scripts are set up to use.</li>
</ul>
</div>
</div>

<div style="text-align:center;margin:2.5rem 0 1rem;">
  <p style="font-size:0.8rem;color:#40668D;font-family:'IBM Plex Mono',monospace;letter-spacing:0.07em;text-transform:uppercase;margin-bottom:0.75rem;">&#8595; click to celebrate &#8595;</p>
  <button id="celebrate-btn" onclick="launchCelebration()" style="
    background: #111;
    color: #fff;
    border: none;
    border-radius: 12px;
    padding: 14px 36px;
    font-size: 1.15rem;
    font-weight: 700;
    font-family: 'IBM Plex Mono', monospace;
    cursor: pointer;
    letter-spacing: 0.04em;
    box-shadow: 0 4px 18px rgba(0,0,0,0.25);
    transition: transform 0.15s, box-shadow 0.15s;
  "
  onmouseover="this.style.transform='scale(1.07)';this.style.boxShadow='0 6px 28px rgba(0,0,0,0.4)'"
  onmouseout="this.style.transform='scale(1)';this.style.boxShadow='0 4px 18px rgba(0,0,0,0.25)'"
  ><img src="images/langchain-symbol.png" style="height:1.1em;vertical-align:middle;margin-right:8px;filter:brightness(0) invert(1);opacity:0.9"> I ran my first deep agent!</button>
</div>

<canvas id="fireworks-canvas" style="position:fixed;top:0;left:0;width:100%;height:100%;pointer-events:none;z-index:9999;display:none;"></canvas>

<div id="celebration-msg" style="
  display:none;
  position:fixed;
  top:50%;left:50%;
  transform:translate(-50%,-50%);
  z-index:10000;
  text-align:center;
  pointer-events:none;
">
  <div style="
    background:#CCE9FF;
    border:2px solid #7FC8FF;
    border-radius:20px;
    padding:2rem 3rem;
    color:#030710;
    font-family:'IBM Plex Mono',monospace;
    box-shadow:0 8px 48px rgba(0,0,0,0.15);
  ">
    <img src="images/langchain-symbol.png" style="width:56px;height:56px;margin-bottom:0.75rem;filter:brightness(0)">
    <div style="font-size:1.5rem;font-weight:700;margin-bottom:0.4rem;">Your first deep agent is live!</div>
    <div style="font-size:0.95rem;opacity:0.85;">Welcome to the world of agentic AI.</div>
  </div>
</div>

<script>
(function () {
  var canvas, ctx, particles = [], running = false, animId;

  var COLORS = [
    '#006DDD','#3399FF','#7FC8FF','#CCE9FF',
    '#0E9F6E','#34D399','#99F6E4',
    '#ffffff','#B8D4FF','#60B0FF'
  ];

  function Particle(x, y) {
    var angle = Math.random() * Math.PI * 2;
    var speed = 2 + Math.random() * 7;
    this.x = x; this.y = y;
    this.vx = Math.cos(angle) * speed;
    this.vy = Math.sin(angle) * speed - 3;
    this.alpha = 1;
    this.decay = 0.012 + Math.random() * 0.018;
    this.radius = 2 + Math.random() * 4;
    this.color = COLORS[Math.floor(Math.random() * COLORS.length)];
    this.trail = [];
    this.shape = Math.random() < 0.3 ? 'star' : 'circle';
  }

  Particle.prototype.update = function () {
    this.trail.push({x: this.x, y: this.y});
    if (this.trail.length > 7) this.trail.shift();
    this.vy += 0.12;
    this.vx *= 0.99;
    this.x += this.vx; this.y += this.vy;
    this.alpha -= this.decay;
  };

  Particle.prototype.draw = function (ctx) {
    ctx.save();
    for (var i = 0; i < this.trail.length; i++) {
      var t = this.trail[i];
      var a = (i / this.trail.length) * this.alpha * 0.4;
      ctx.globalAlpha = a;
      ctx.fillStyle = this.color;
      ctx.beginPath();
      ctx.arc(t.x, t.y, this.radius * 0.5, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.globalAlpha = this.alpha;
    ctx.fillStyle = this.color;
    if (this.shape === 'star') {
      drawStar(ctx, this.x, this.y, 5, this.radius * 1.4, this.radius * 0.6);
    } else {
      ctx.beginPath();
      ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
  };

  function drawStar(ctx, cx, cy, spikes, outer, inner) {
    var rot = (Math.PI / 2) * 3, step = Math.PI / spikes;
    ctx.beginPath();
    ctx.moveTo(cx, cy - outer);
    for (var i = 0; i < spikes; i++) {
      ctx.lineTo(cx + Math.cos(rot) * outer, cy + Math.sin(rot) * outer); rot += step;
      ctx.lineTo(cx + Math.cos(rot) * inner, cy + Math.sin(rot) * inner); rot += step;
    }
    ctx.lineTo(cx, cy - outer);
    ctx.closePath();
    ctx.fill();
  }

  function burst(x, y, count) {
    for (var i = 0; i < count; i++) particles.push(new Particle(x, y));
  }

  function randomBurst() {
    var x = 0.1 * window.innerWidth + Math.random() * 0.8 * window.innerWidth;
    var y = 0.1 * window.innerHeight + Math.random() * 0.55 * window.innerHeight;
    burst(x, y, 60 + Math.floor(Math.random() * 50));
  }

  function loop() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    particles = particles.filter(function (p) { return p.alpha > 0; });
    particles.forEach(function (p) { p.update(); p.draw(ctx); });
    if (running || particles.length > 0) animId = requestAnimationFrame(loop);
    else { canvas.style.display = 'none'; }
  }

  window.launchCelebration = function () {
    if (!canvas) {
      canvas = document.getElementById('fireworks-canvas');
      ctx = canvas.getContext('2d');
    }
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    canvas.style.display = 'block';

    var msg = document.getElementById('celebration-msg');
    msg.style.display = 'block';
    msg.style.animation = 'none';
    msg.style.opacity = '1';
    setTimeout(function () {
      msg.style.transition = 'opacity 1s';
      msg.style.opacity = '0';
      setTimeout(function () { msg.style.display = 'none'; }, 1000);
    }, 3200);

    var btn = document.getElementById('celebrate-btn');
    btn.textContent = 'Again!';

    particles = [];
    running = true;

    var shots = 0, maxShots = 22;
    function fire() {
      if (shots >= maxShots) { running = false; return; }
      var count = Math.floor(Math.random() * 3) + 1;
      for (var i = 0; i < count; i++) randomBurst();
      shots += count;
      setTimeout(fire, 120 + Math.random() * 280);
    }

    if (animId) cancelAnimationFrame(animId);
    fire();
    loop();
  };
})();
</script>

---

## References

**Documentation:**

- [Deep Agents overview](https://docs.langchain.com/oss/python/deepagents/overview)
- [Models (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/models)
- [deepagents README (GitHub)](https://github.com/langchain-ai/deepagents)
