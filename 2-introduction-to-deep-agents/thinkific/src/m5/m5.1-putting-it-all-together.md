[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-lessons/deepagents/m5/m5.1-putting-it-all-together.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

# Putting It All Together

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/phWZWmbUpx5" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

## The project: a sales assistant for Chinook

The capstone we'll build is a **sales assistant** for **Jane Peacock**, a sales rep at **Chinook**, an online music distributor. It helps her work her "book of business": it reads quote requests from a **mock Gmail-style inbox**, looks up customers and prices in the **Chinook sales database**, drafts replies for her to approve, writes a weekly music newsletter, and reports on how her territory is doing.

It's a single deep agent, but a complete, realistic application.

---

Over the last four modules you met the pieces of Deep Agents one at a time: the model and tools, skills and `AGENTS.md`, the filesystem, the interpreter, the sandbox, human-in-the-loop, and subagents. This module is the capstone: a single, real project that uses **all** of them together.

A key takeaway from this module: creating a deep agent requires very little code. Most of the agent's behavior is determined by **text**: the skills and the `AGENTS.md` file.

<div style="text-align: center;">
<svg viewBox="0 0 720 296" width="680" role="img" aria-label="Two inputs feed a deep agent. On the left, a card labeled 'A little code' lists tools, subagents, and the create_deep_agent call. Below it, a card labeled 'Some text files' lists AGENTS.md and skills. Both flow right into a node labeled 'Your deep agent', which flows on to 'langgraph dev → served over HTTP'." xmlns="http://www.w3.org/2000/svg" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">
  <rect width="720" height="296" fill="#030710" rx="8"/>
  <text x="360" y="34" text-anchor="middle" font-size="17" font-weight="300" fill="#F2FAFF">What it takes to build a deep agent</text>
  <text x="360" y="56" text-anchor="middle" font-size="13" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace">a little code + some text files</text>

  <!-- Card: code -->
  <rect x="36" y="80" width="262" height="100" rx="8" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="56" y="108" font-size="15" font-weight="600" fill="#7FC8FF">A little code</text>
  <text x="56" y="131" font-size="13" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">• tools (plain functions)</text>
  <text x="56" y="151" font-size="13" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">• subagents (small dicts)</text>
  <text x="56" y="171" font-size="13" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">• the create_deep_agent() call</text>

  <!-- Card: text files -->
  <rect x="36" y="200" width="262" height="84" rx="8" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="56" y="228" font-size="15" font-weight="600" fill="#7FC8FF">Mostly text</text>
  <text x="56" y="251" font-size="13" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">• AGENTS.md (operating manual)</text>
  <text x="56" y="271" font-size="13" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">• skills/ (a playbook per task)</text>

  <!-- arrows to agent -->
  <line x1="298" y1="130" x2="448" y2="172" stroke="#40668D" stroke-width="1.5"/>
  <line x1="298" y1="242" x2="448" y2="190" stroke="#40668D" stroke-width="1.5"/>

  <!-- agent node -->
  <rect x="450" y="142" width="232" height="80" rx="8" fill="#0D1322" stroke="#7FC8FF" stroke-width="1.5"/>
  <text x="566" y="178" text-anchor="middle" font-size="16" font-weight="600" fill="#F2FAFF">Your deep agent</text>
  <text x="566" y="200" text-anchor="middle" font-size="13" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">create_deep_agent(...)</text>

  <!-- deploy -->
  <line x1="566" y1="222" x2="566" y2="258" stroke="#40668D" stroke-width="1.5"/>
  <text x="566" y="280" text-anchor="middle" font-size="13" fill="#7FC8FF" font-family="'IBM Plex Mono', ui-monospace, monospace">langgraph dev → served over HTTP</text>
</svg>

<p><em>A little code plus a few Markdown files, joined by one <code>create_deep_agent()</code> call.</em></p>

</div>

---

## Every feature, and where this project uses it

Here's the whole toolkit from the course, mapped to where the sales assistant puts each one to work:

- **Subagents**: `chinook-analyst` (owns the database), `inbox-manager` (owns mail), `quote-reviewer` (sanity-checks a quote), and, when web search is configured, `genre-researcher` (newsletter research). Each is a specialist with its own tools.
- **Parallel subagents**: with web search configured, the newsletter fans out one `genre-researcher` per genre, all at once, then stitches the segments together.
- **Skills + `AGENTS.md`**: a plain-text operating manual plus one playbook per task (RFQ quote, newsletter, territory report).
- **Filesystem**: skills, `AGENTS.md`, the quotes ledger, newsletter HTML, and territory report live in the assistant's local `FilesystemBackend` during local development.
- **Interpreter**: exact quote math: line totals and the volume discount, computed rather than guessed.
- **Human-in-the-loop**: approval gates on the two consequential actions: saving a mail draft and adding a new customer.
- **MCP**: mail access through the local mock MCP server used in this module. A real Gmail/OAuth integration would be a later extension, not part of the shipped lab.
- **Todo list** *(optional)*: `write_todos`, opt-in via `TodoListMiddleware`, can sequence a multi-step task like the RFQ flow so the agent keeps its place.

Over the rest of the module, you'll build this assistant and run it.

---

## Next

Before building the assistant, the next lesson covers **deploying** an agent locally with `langgraph dev`, serving it over HTTP so a chat UI can talk to it.

---

## References

**Documentation:**

- [Deep Agents overview](https://docs.langchain.com/oss/python/deepagents/overview)
- [Skills (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/skills)
- [Subagents (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/subagents)
