[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-lessons/deepagents/m5/m5.2-local-deployment.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

<div class="page-lang-switch" data-page-lang-switch>
  <button class="page-lang-btn" data-page-lang="python" aria-selected="false">Python</button>
  <button class="page-lang-btn" data-page-lang="typescript" aria-selected="false">TypeScript</button>
</div>

# Local Deployment

<style>
.lt-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin: 28px 0 0;
  border-bottom: 2px solid #CCE9FF;
}
.lt-group { display: flex; gap: 3px; }
.lt-dep  { --c: #006DDD; }
.lt-quiz { --c: #7C3AED; }
.lt-lab  { --c: #B45309; }
.lt-homework { --c: #0E9F6E; }
.lt-tab {
  font: 500 14px 'IBM Plex Mono', monospace;
  padding: 9px 14px;
  border: none;
  background: transparent;
  color: #40668D;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  border-radius: 6px 6px 0 0;
  transition: background .15s, color .15s, border-color .15s;
  white-space: nowrap;
}
.lt-tab:hover { background: #F2FAFF; color: #030710; }
.lt-tab.active {
  color: var(--c);
  border-bottom-color: var(--c);
  background: #fff;
}
.lt-panel { display: none; padding-top: 24px; }
.lt-panel.active { display: block; }
@media (max-width: 600px) {
  .lt-bar { flex-wrap: nowrap; overflow-x: auto; gap: 12px; }
  .lt-tab { padding: 8px 10px; font-size: 13px; }
}
</style>

<div class="lt-bar" role="tablist" aria-label="Lesson sections">
  <div class="lt-group lt-dep">
    <button class="lt-tab active" data-p="lesson" role="tab" aria-selected="true">Local Deployment</button>
  </div>
  <div class="lt-group lt-lab">
    <button class="lt-tab" data-p="lab" role="tab" aria-selected="false">Lab</button>
  </div>
  <div class="lt-group lt-quiz">
    <button class="lt-tab" data-p="quiz" role="tab" aria-selected="false">Quiz</button>
  </div>
  <div class="lt-group lt-homework">
    <button class="lt-tab" data-p="homework" role="tab" aria-selected="false">Homework</button>
  </div>
</div>

<div class="lt-panel active" id="p-lesson" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/6m7uJv3WmbO" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

So far you've run every agent as a Python script that calls `agent.invoke(...)` and prints the result. That's a great fit for one-off tasks. The Chinook Sales Assistant you'll build and run next has different requirements. It has a chat user interface that connects to the agent over HTTP. That means it must remain available over time and accept requests over the web, so it runs as a **service** rather than a one-time script.

<div style="text-align: center;">
<svg viewBox="0 0 720 180" width="720" role="img" aria-label="A box labeled 'Chat UI' on the left connects by an arrow labeled HTTP to a box labeled 'Agent' on the right." xmlns="http://www.w3.org/2000/svg" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">
  <rect width="720" height="180" fill="#030710" rx="8"/>
  <text x="360" y="36" text-anchor="middle" font-size="17" font-weight="300" fill="#F2FAFF">Reaching the agent over the web</text>
  <rect x="70" y="84" width="180" height="74" rx="8" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="160" y="126" text-anchor="middle" font-size="16" font-weight="600" fill="#7FC8FF">Chat UI</text>
  <rect x="470" y="84" width="180" height="74" rx="8" fill="#0D1322" stroke="#7FC8FF" stroke-width="1.5"/>
  <text x="560" y="126" text-anchor="middle" font-size="16" font-weight="600" fill="#F2FAFF">Agent</text>
  <line x1="250" y1="121" x2="455" y2="121" stroke="#40668D" stroke-width="1.5"/>
  <polygon points="455,114 470,121 455,128" fill="#40668D"/>
  <text x="360" y="112" text-anchor="middle" font-size="14" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">HTTP</text>
</svg>

<p><em>The agent runs behind a web API, not inside your Python script.</em></p>

</div>

---

## From a script to a service

`langgraph dev` (part of the `langgraph-cli` package) provides a cloud-deployment-like environment on your local machine: it hosts your agent behind an HTTP API and runs the supporting infrastructure around it.

<div style="text-align: center;">
<svg viewBox="0 0 720 300" width="720" role="img" aria-label="A box labeled 'Studio (chat + debug UI)' on the left connects by an arrow labeled HTTP to a dashed box labeled 'langgraph dev'. Inside that box is a box labeled 'Your deep agent' at the top, and below it a row of four built-in service tiles: HTTP API (endpoints), Persistence (threads), Store (memory), and Task queue (runs)." xmlns="http://www.w3.org/2000/svg" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">
  <rect width="720" height="300" fill="#030710" rx="8"/>
  <text x="360" y="34" text-anchor="middle" font-size="17" font-weight="300" fill="#F2FAFF">Your agent runs inside langgraph dev</text>

  <!-- Studio (client) -->
  <rect x="24" y="128" width="150" height="76" rx="8" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="99" y="160" text-anchor="middle" font-size="16" font-weight="600" fill="#7FC8FF">Studio</text>
  <text x="99" y="182" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">chat + debug UI</text>

  <!-- HTTP arrow into langgraph dev -->
  <line x1="174" y1="166" x2="205" y2="166" stroke="#40668D" stroke-width="1.5"/>
  <polygon points="205,159 214,166 205,173" fill="#40668D"/>
  <text x="194" y="154" text-anchor="middle" font-size="13" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">HTTP</text>

  <!-- langgraph dev box -->
  <rect x="214" y="64" width="482" height="222" rx="8" fill="#0D1322" stroke="#40668D" stroke-width="1" stroke-dasharray="4 3"/>
  <text x="234" y="90" font-size="14" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">langgraph dev</text>

  <!-- agent node -->
  <rect x="355" y="104" width="200" height="58" rx="8" fill="#161F34" stroke="#7FC8FF" stroke-width="1.5"/>
  <text x="455" y="130" text-anchor="middle" font-size="16" font-weight="600" fill="#F2FAFF">Your deep agent</text>
  <text x="455" y="150" text-anchor="middle" font-size="12" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">create_deep_agent(...)</text>

  <line x1="455" y1="162" x2="455" y2="178" stroke="#40668D" stroke-width="1.5"/>
  <text x="455" y="192" text-anchor="middle" font-size="13" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">built-in services</text>

  <!-- service tiles -->
  <rect x="232" y="204" width="100" height="64" rx="6" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="282" y="234" text-anchor="middle" font-size="14" font-weight="600" fill="#7FC8FF">HTTP API</text>
  <text x="282" y="254" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">endpoints</text>

  <rect x="348" y="204" width="100" height="64" rx="6" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="398" y="234" text-anchor="middle" font-size="14" font-weight="600" fill="#7FC8FF">Persistence</text>
  <text x="398" y="254" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">threads</text>

  <rect x="464" y="204" width="100" height="64" rx="6" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="514" y="234" text-anchor="middle" font-size="14" font-weight="600" fill="#7FC8FF">Store</text>
  <text x="514" y="254" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">memory</text>

  <rect x="580" y="204" width="100" height="64" rx="6" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="630" y="234" text-anchor="middle" font-size="14" font-weight="600" fill="#7FC8FF">Task queue</text>
  <text x="630" y="254" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">runs</text>
</svg>

<p><em>You write the agent. <code>langgraph dev</code> wraps it in an HTTP API with built-in persistence, memory, and a task queue. Studio and any other chat UI reach it over HTTP.</em></p>

</div>

### Studio

`langgraph dev` includes a chat and debugging interface called "Studio". As shown in the diagram, Studio uses the API endpoints provided by `langgraph dev` to allow you to converse with your agent via chat as well as examine and control the underlying agentic loop via a graphical debugger.

---

## The agent's API

Every agent served by `langgraph dev` exposes the same **Agent Server API** ([full reference](https://docs.langchain.com/langsmith/server-api-ref)). Because it's a running web service, the dev server documents itself: once it's up, open **`http://localhost:2024/docs`** in your browser to see the live, interactive API for your agent.

The endpoints group into a handful of categories. Here are the ones you'll use most, with the advanced ones grouped below.

<details>
<summary><strong>Assistants</strong> (a configured instance of a graph)</summary>

<br>

The deployable unit: a graph plus its configuration.

- `POST /assistants`
- `POST /assistants/search`
- `GET /assistants/{id}/graph`
- `GET /assistants/{id}/schemas`

</details>

<details>
<summary><strong>Threads</strong> (the accumulated state of a conversation)</summary>

<br>

A thread holds the accumulated outputs of a group of runs, forming the persistent conversation.

- `POST /threads`
- `GET /threads/{id}/state`
- `GET /threads/{id}/history`
- `POST /threads/search`

</details>

<details>
<summary><strong>Thread Runs</strong> (one invocation of the agent on a thread)</summary>

<br>

A run invokes the graph on a thread and updates its state.

- `POST /threads/{id}/runs`
- `POST /threads/{id}/runs/stream`
- `POST /threads/{id}/runs/wait`
- `POST /threads/{id}/runs/{run_id}/cancel`

</details>

<details>
<summary><strong>Store</strong> (long-term key-value memory)</summary>

<br>

A persistent key-value store (long-term memory) available from any thread.

- `PUT /store/items`
- `GET /store/items`
- `POST /store/items/search`
- `GET /store/namespaces`

</details>

<details>
<summary>Advanced (Stateless Runs, Crons, Streaming, A2A, MCP, System)</summary>

<br>

- **Stateless Runs**: a run with no state or memory persistence: `POST /runs`, `POST /runs/stream`, `POST /runs/wait`, `POST /runs/batch`
- **Crons**: a periodic run on a schedule: `POST /runs/crons`, `POST /threads/{id}/runs/crons`
- **Streaming**: thread-centric streaming over SSE+HTTP (WebSocket also supported): `POST /threads/{id}/stream`, `/threads/{id}/stream/events`
- **A2A**: expose an assistant as an A2A-compliant agent: `POST /a2a/{assistant_id}`
- **MCP**: expose the agent as an MCP server: `/mcp/`
- **System**: health, metrics, server info: `GET /ok`, `GET /info`, `GET /metrics`, `GET /docs`

</details>

---

## Beyond local deployment

`langgraph dev` is intended for local development. Deploying an agent to production as a managed Cloud service, a standalone server, or a fully self-hosted platform introduces additional infrastructure and operational considerations beyond the scope of this lesson. The following resources cover those options in more detail:

- [**Deployment options**](https://docs.langchain.com/langsmith/deployment): the docs overview of the available hosting models (Cloud, standalone server, and self-hosted) and what each one manages for you.
- [**LangSmith Deployment**](https://academy.langchain.com/courses/langsmith-deployment): a LangChain Academy course on deploying, managing, and controlling your agents in production.

---

## Recap

In this lesson you saw what it means to *deploy* an agent locally:

- A deployed agent runs as a **service reached over HTTP**, not a script you `invoke()` in Python.
- **`langgraph dev`** hosts your agent, exposes it as an **API**, and uses a small **`langgraph.json`** config file to declare which graph to serve.
- The **Agent Server API** is the same for every agent: **Assistants**, **Threads**, **Thread Runs**, and **Store**, plus advanced endpoints. The running server documents itself at **`localhost:2024/docs`**.
- **`langgraph dev` ships LangGraph Studio** as a client for that API, a combined **chat + debug** interface. Any other chat UI reaches the agent the same way, over HTTP.

Try it yourself in the **Lab** tab above.

---

## Next

Complete the **Quiz** and **Lab** above, then continue to the capstone: **the Chinook Sales Assistant**.

---

## References

**Documentation:**

- [Run a local server (`langgraph dev`)](https://docs.langchain.com/oss/python/langgraph/local-server)
- [LangGraph Studio](https://docs.langchain.com/oss/python/langgraph/studio)
- [Agent Server API reference](https://docs.langchain.com/langsmith/server-api-ref)
- [LangGraph SDK overview](https://docs.langchain.com/langgraph-platform/sdk)

**LangGraph SDK API reference:**

- [Python SDK](https://reference.langchain.com/python/langgraph-sdk)
- [TypeScript SDK](https://reference.langchain.com/javascript/langchain-langgraph-sdk)

</div>

<div data-lang="typescript" markdown="1">

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/6m7uJv3WmbO" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

So far you've run every agent as a TypeScript script that calls `agent.invoke(...)` and prints the result. That's a great fit for one-off tasks. The Chinook Sales Assistant you'll build and run next has different requirements. It has a chat user interface that connects to the agent over HTTP. That means it must remain available over time and accept requests over the web, so it runs as a **service** rather than a one-time script.

<div style="text-align: center;">
<svg viewBox="0 0 720 180" width="720" role="img" aria-label="A box labeled 'Chat UI' on the left connects by an arrow labeled HTTP to a box labeled 'Agent' on the right." xmlns="http://www.w3.org/2000/svg" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">
  <rect width="720" height="180" fill="#030710" rx="8"/>
  <text x="360" y="36" text-anchor="middle" font-size="17" font-weight="300" fill="#F2FAFF">Reaching the agent over the web</text>
  <rect x="70" y="84" width="180" height="74" rx="8" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="160" y="126" text-anchor="middle" font-size="16" font-weight="600" fill="#7FC8FF">Chat UI</text>
  <rect x="470" y="84" width="180" height="74" rx="8" fill="#0D1322" stroke="#7FC8FF" stroke-width="1.5"/>
  <text x="560" y="126" text-anchor="middle" font-size="16" font-weight="600" fill="#F2FAFF">Agent</text>
  <line x1="250" y1="121" x2="455" y2="121" stroke="#40668D" stroke-width="1.5"/>
  <polygon points="455,114 470,121 455,128" fill="#40668D"/>
  <text x="360" y="112" text-anchor="middle" font-size="14" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">HTTP</text>
</svg>

<p><em>The agent runs behind a web API, not inside your TypeScript script.</em></p>

</div>

---

## From a script to a service

`langgraphjs dev` (part of the `@langchain/langgraph-cli` package) provides a cloud-deployment-like environment on your local machine: it hosts your agent behind an HTTP API and runs the supporting infrastructure around it.

<div style="text-align: center;">
<svg viewBox="0 0 720 300" width="720" role="img" aria-label="A box labeled 'Studio (chat + debug UI)' on the left connects by an arrow labeled HTTP to a dashed box labeled 'langgraphjs dev'. Inside that box is a box labeled 'Your deep agent' at the top, and below it a row of four built-in service tiles: HTTP API (endpoints), Persistence (threads), Store (memory), and Task queue (runs)." xmlns="http://www.w3.org/2000/svg" font-family="'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif">
  <rect width="720" height="300" fill="#030710" rx="8"/>
  <text x="360" y="34" text-anchor="middle" font-size="17" font-weight="300" fill="#F2FAFF">Your agent runs inside langgraphjs dev</text>

  <!-- Studio (client) -->
  <rect x="24" y="128" width="150" height="76" rx="8" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="99" y="160" text-anchor="middle" font-size="16" font-weight="600" fill="#7FC8FF">Studio</text>
  <text x="99" y="182" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">chat + debug UI</text>

  <!-- HTTP arrow into langgraphjs dev -->
  <line x1="174" y1="166" x2="205" y2="166" stroke="#40668D" stroke-width="1.5"/>
  <polygon points="205,159 214,166 205,173" fill="#40668D"/>
  <text x="194" y="154" text-anchor="middle" font-size="13" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">HTTP</text>

  <!-- langgraphjs dev box -->
  <rect x="214" y="64" width="482" height="222" rx="8" fill="#0D1322" stroke="#40668D" stroke-width="1" stroke-dasharray="4 3"/>
  <text x="234" y="90" font-size="14" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">langgraphjs dev</text>

  <!-- agent node -->
  <rect x="355" y="104" width="200" height="58" rx="8" fill="#161F34" stroke="#7FC8FF" stroke-width="1.5"/>
  <text x="455" y="130" text-anchor="middle" font-size="16" font-weight="600" fill="#F2FAFF">Your deep agent</text>
  <text x="455" y="150" text-anchor="middle" font-size="12" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">createDeepAgent(...)</text>

  <line x1="455" y1="162" x2="455" y2="178" stroke="#40668D" stroke-width="1.5"/>
  <text x="455" y="192" text-anchor="middle" font-size="13" fill="#99D3FF" font-family="'IBM Plex Mono', ui-monospace, monospace">built-in services</text>

  <!-- service tiles -->
  <rect x="232" y="204" width="100" height="64" rx="6" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="282" y="234" text-anchor="middle" font-size="14" font-weight="600" fill="#7FC8FF">HTTP API</text>
  <text x="282" y="254" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">endpoints</text>

  <rect x="348" y="204" width="100" height="64" rx="6" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="398" y="234" text-anchor="middle" font-size="14" font-weight="600" fill="#7FC8FF">Persistence</text>
  <text x="398" y="254" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">threads</text>

  <rect x="464" y="204" width="100" height="64" rx="6" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="514" y="234" text-anchor="middle" font-size="14" font-weight="600" fill="#7FC8FF">Store</text>
  <text x="514" y="254" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">memory</text>

  <rect x="580" y="204" width="100" height="64" rx="6" fill="#161F34" stroke="#40668D" stroke-width="1"/>
  <text x="630" y="234" text-anchor="middle" font-size="14" font-weight="600" fill="#7FC8FF">Task queue</text>
  <text x="630" y="254" text-anchor="middle" font-size="12" fill="#CCE9FF" font-family="'IBM Plex Mono', ui-monospace, monospace">runs</text>
</svg>

<p><em>You write the agent. <code>langgraphjs dev</code> wraps it in an HTTP API with built-in persistence, memory, and a task queue. Studio and any other chat UI reach it over HTTP.</em></p>

</div>

### Studio

`langgraphjs dev` includes a chat and debugging interface called "Studio". As shown in the diagram, Studio uses the API endpoints provided by `langgraphjs dev` to allow you to converse with your agent via chat as well as examine and control the underlying agentic loop via a graphical debugger.

---

## The agent's API

Every agent served by `langgraphjs dev` exposes the same **Agent Server API** ([full reference](https://docs.langchain.com/langsmith/server-api-ref)). Because it's a running web service, the dev server documents itself: once it's up, open **`http://localhost:2024/docs`** in your browser to see the live, interactive API for your agent.

The endpoints group into a handful of categories. Here are the ones you'll use most, with the advanced ones grouped below.

<details>
<summary><strong>Assistants</strong> (a configured instance of a graph)</summary>

<br>

The deployable unit: a graph plus its configuration.

- `POST /assistants`
- `POST /assistants/search`
- `GET /assistants/{id}/graph`
- `GET /assistants/{id}/schemas`

</details>

<details>
<summary><strong>Threads</strong> (the accumulated state of a conversation)</summary>

<br>

A thread holds the accumulated outputs of a group of runs, forming the persistent conversation.

- `POST /threads`
- `GET /threads/{id}/state`
- `GET /threads/{id}/history`
- `POST /threads/search`

</details>

<details>
<summary><strong>Thread Runs</strong> (one invocation of the agent on a thread)</summary>

<br>

A run invokes the graph on a thread and updates its state.

- `POST /threads/{id}/runs`
- `POST /threads/{id}/runs/stream`
- `POST /threads/{id}/runs/wait`
- `POST /threads/{id}/runs/{run_id}/cancel`

</details>

<details>
<summary><strong>Store</strong> (long-term key-value memory)</summary>

<br>

A persistent key-value store (long-term memory) available from any thread.

- `PUT /store/items`
- `GET /store/items`
- `POST /store/items/search`
- `GET /store/namespaces`

</details>

<details>
<summary>Advanced (Stateless Runs, Crons, Streaming, A2A, MCP, System)</summary>

<br>

- **Stateless Runs**: a run with no state or memory persistence: `POST /runs`, `POST /runs/stream`, `POST /runs/wait`, `POST /runs/batch`
- **Crons**: a periodic run on a schedule: `POST /runs/crons`, `POST /threads/{id}/runs/crons`
- **Streaming**: thread-centric streaming over SSE+HTTP (WebSocket also supported): `POST /threads/{id}/stream`, `/threads/{id}/stream/events`
- **A2A**: expose an assistant as an A2A-compliant agent: `POST /a2a/{assistant_id}`
- **MCP**: expose the agent as an MCP server: `/mcp/`
- **System**: health, metrics, server info: `GET /ok`, `GET /info`, `GET /metrics`, `GET /docs`

</details>

---

## Beyond local deployment

`langgraphjs dev` is intended for local development. Deploying an agent to production as a managed Cloud service, a standalone server, or a fully self-hosted platform introduces additional infrastructure and operational considerations beyond the scope of this lesson. The following resources cover those options in more detail:

- [**Deployment options**](https://docs.langchain.com/langsmith/deployment): the docs overview of the available hosting models (Cloud, standalone server, and self-hosted) and what each one manages for you.
- [**LangSmith Deployment**](https://academy.langchain.com/courses/langsmith-deployment): a LangChain Academy course on deploying, managing, and controlling your agents in production.

---

## Recap

In this lesson you saw what it means to *deploy* an agent locally:

- A deployed agent runs as a **service reached over HTTP**, not a script you `invoke()` in TypeScript.
- **`langgraphjs dev`** hosts your agent, exposes it as an **API**, and uses a small **`langgraph.json`** config file to declare which graph to serve.
- The **Agent Server API** is the same for every agent: **Assistants**, **Threads**, **Thread Runs**, and **Store**, plus advanced endpoints. The running server documents itself at **`localhost:2024/docs`**.
- **`langgraphjs dev` ships LangGraph Studio** as a client for that API, a combined **chat + debug** interface. Any other chat UI reaches the agent the same way, over HTTP.

Try it yourself in the **Lab** tab above.

---

## Next

Complete the **Quiz** and **Lab** above, then continue to the capstone: **the Chinook Sales Assistant**.

---

## References

**Documentation:**

- [Run a local server (`langgraphjs dev`)](https://docs.langchain.com/oss/javascript/langgraph/local-server)
- [LangGraph Studio](https://docs.langchain.com/oss/javascript/langgraph/studio)
- [Agent Server API reference](https://docs.langchain.com/langsmith/server-api-ref)
- [LangGraph SDK overview](https://docs.langchain.com/langgraph-platform/sdk)

**LangGraph SDK API reference:**

- [Python SDK](https://reference.langchain.com/python/langgraph-sdk)
- [TypeScript SDK](https://reference.langchain.com/javascript/langchain-langgraph-sdk)

</div>

</div>

<div class="lt-panel" id="p-quiz" role="tabpanel">

<h2>Check your understanding</h2>

<div data-lang="python">
<MCQ
    question="What does it mean to deploy an agent with langgraph dev?"
    choices='["The agent runs as a service reached over an HTTP API, rather than a script you invoke in Python", "The agent is compiled into a standalone binary you distribute to users", "The agent is uploaded to LangSmith Cloud automatically", "The agent can only be run from inside LangGraph Studio"]'
    correctIndex={0}
    explanation="A deployed agent runs as a service: langgraph dev hosts it behind an HTTP API, and clients reach it over HTTP instead of calling agent.invoke() in a Python script."
/>
</div>

<div data-lang="typescript">
<MCQ
    question="What does it mean to deploy an agent with langgraphjs dev?"
    choices='["The agent runs as a service reached over an HTTP API, rather than a script you invoke in TypeScript", "The agent is compiled into a standalone binary you distribute to users", "The agent is uploaded to LangSmith Cloud automatically", "The agent can only be run from inside LangGraph Studio"]'
    correctIndex={0}
    explanation="A deployed agent runs as a service: langgraphjs dev hosts it behind an HTTP API, and clients reach it over HTTP instead of calling agent.invoke() in a TypeScript script."
/>
</div>

<div data-lang="python">
<MCQ
    question="What is the role of langgraph.json?"
    choices='["It stores the conversation history for each thread", "It holds your API keys and secrets", "langgraph.json configures many aspects of a deployment including which graph langgraph dev should serve, where its dependencies are, and which env file to load", "It defines the HTTP endpoints the server exposes"]'
    correctIndex={2}
    explanation="langgraph.json configures many aspects of a deployment including which graph to serve (e.g. ./agent.py:graph), where its dependencies are, and which env file to load. The server derives its endpoints automatically."
/>
</div>

<div data-lang="typescript">
<MCQ
    question="What is the role of langgraph.json?"
    choices='["It stores the conversation history for each thread", "It holds your API keys and secrets", "langgraph.json configures many aspects of a deployment including which graph langgraphjs dev should serve, where its dependencies are, and which env file to load", "It defines the HTTP endpoints the server exposes"]'
    correctIndex={2}
    explanation="langgraph.json configures many aspects of a deployment including which graph to serve (e.g. ./agent.ts:graph), where its dependencies are, and which env file to load. The server derives its endpoints automatically."
/>
</div>

<MCQ
    question="In the Agent Server API, what is a Thread?"
    choices=’["A persistent record of a conversation's accumulated state", "A single invocation of the graph", "A configured instance of a graph", "A background process that runs the agent"]’
    correctIndex={0}
    explanation="A Thread holds the accumulated state of a conversation across multiple runs. A Run is one invocation; an Assistant is a configured graph instance."
/>

<MCQ
    question="What is LangGraph Studio?"
    choices='["A separate runtime that replaces langgraph dev in production", "A client for the Agent Server API, providing a combined chat and graphical debugging interface over HTTP", "A command-line tool for editing langgraph.json", "A hosted cloud service that deploys your agent"]'
    correctIndex={1}
    explanation="Studio is a client of the same Agent Server API. It provides both a chat interface and a graphical debugger, and it reaches the agent over HTTP, exactly as any other chat UI would."
/>

</div>

<div class="lt-panel" id="p-lab" markdown="1" role="tabpanel">

## Lab: Deploy a Basic Agent

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lab + Studio Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/kVx79ZeFLVY" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

You'll build a simple agent.

<div data-lang="python" markdown="1">

The agent itself is a single `create_deep_agent` call. It pulls `model` from the course's shared `models.py`, so the agent uses whatever provider you configured during setup, and is exposed as a module-level `graph` so the server can find it:

```python
# python/m5/hello/agent.py
from deepagents import create_deep_agent
from models import model

# langgraph.json points at this variable: "./agent.py:graph"
graph = create_deep_agent(model=model)
```

Next to it, a **`langgraph.json`** tells `langgraph dev` which graph to serve, where its dependencies are, and which env file to load (`python/m5/hello/langgraph.json`):

```json
{
  "dependencies": ["../.."],
  "graphs": {
    "agent": "./agent.py:graph"
  },
  "env": "../../.env"
}
```

</div>

<div data-lang="typescript" markdown="1">

The agent itself is a single `createDeepAgent` call. It pulls `model` from the course's shared `models.ts`, so the agent uses whatever provider you configured during setup, and is exposed as a module-level `graph` so the server can find it:

```typescript
// typescript/m5/hello/agent.ts
import { createDeepAgent } from "deepagents";

import { model } from "../../models.js";

// `langgraph.json` points at this module-level export: "./agent.ts:graph"
export const graph = createDeepAgent({ model });
```

Next to it, a **`langgraph.json`** tells `langgraphjs dev` which graph to serve, where its dependencies are, and which env file to load (`typescript/m5/hello/langgraph.json`):

```json
{
  "dependencies": ["../.."],
  "graphs": {
    "agent": "./agent.ts:graph"
  },
  "env": "../../.env"
}
```

</div>

That's the entire deployment: one graph, declared in one config file. Now serve it:

<div data-lang="python" markdown="1">

Before running it, make sure your model API key from setup is available in the Python environment.

```bash
cd python/m5/hello
uv run langgraph dev
```

</div>

<div data-lang="typescript" markdown="1">

Before running it, make sure your model API key from setup is available in the TypeScript environment.

```bash
cd typescript/m5/hello
pnpm exec langgraphjs dev
```

</div>

The server starts on `http://localhost:2024` and **opens LangGraph Studio in your browser** automatically. In Studio you can:

- **Chat with the agent** in the conversation panel: type a message and watch it reply.
- **Walk through the state machine**: the graph view shows each node; step through a run to see state change as the agent works.

---

</div>

<div class="lt-panel" id="p-homework" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

## Deploy Your Own Agent

The lab deployed a fairly bare-bones agent (no tools, no persona, just `create_deep_agent(model=model)`), and you only ever talked to it through Studio's chat panel.

This homework has two parts:

1. Deploy an agent with your personal touch.
2. Talk to it the way any other client would, straight over the Agent Server API this lesson covers, instead of through Studio.

Open `python/m5/homework/agent.py`.

1. **TODO 1**: write your own `@tool`-decorated function. Pick any topic of your own choosing and give the tool a small lookup of facts to draw from. A plain Python dict is enough, no external API or key required.
2. **TODO 2**: write a system prompt that gives your agent a persona of your choosing and tells it to call your tool before answering.

### Run it

```bash
cd python/m5/homework
uv run langgraph dev
```

Chat with your agent in the Studio window that opens, the same way you did in the Lab. Leave that running, then open `python/m5/homework/call_agent_api.py` in a second terminal.

3. **TODO 3**: write a question that should make your agent call the tool you wrote for TODO 1. The script creates a thread and starts a run against your running server using the LangGraph SDK, the same `Threads` and `Runs` endpoints described earlier in this lesson, and prints back the response it gets over HTTP.

```bash
cd python
uv run ./m5/homework/call_agent_api.py
```

Stuck, or want to see the finished shape before you start? `python/m5/homework_filled/` is a reference copy with every TODO filled in (a storm-chaser persona reporting on extreme weather records, queried both via Studio and via `call_agent_api.py`); run it the same way (`cd python/m5/homework_filled && uv run langgraph dev`, then `uv run ./m5/homework_filled/call_agent_api.py` from `python/`) to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

</div>

<div data-lang="typescript" markdown="1">

## Deploy Your Own Agent

The lab deployed a fairly bare-bones agent (no tools, no persona, just `createDeepAgent({ model })`), and you only ever talked to it through Studio's chat panel.

This homework has two parts:

1. Deploy an agent with your personal touch.
2. Talk to it the way any other client would, straight over the Agent Server API this lesson covers, instead of through Studio.

Open `typescript/m5/homework/agent.ts`.

1. **TODO 1**: write your own tool with the `tool()` helper on a topic of your choosing. Pick any topic of your own choosing and give the tool a small lookup of facts to draw from. A plain object is enough, no external API or key required.
2. **TODO 2**: write a `systemPrompt` that gives your agent a persona of your choosing and tells it to call your tool before answering.

### Run it

```bash
cd typescript/m5/homework
pnpm exec langgraphjs dev
```

Chat with your agent in the Studio window that opens, the same way you did in the Lab. Leave that running, then open `typescript/m5/homework/call_agent_api.ts` in a second terminal.

3. **TODO 3**: write a question that should make your agent call the tool you wrote for TODO 1. The script creates a thread and starts a run against your running server using the LangGraph SDK, the same `Threads` and `Runs` endpoints described earlier in this lesson, and prints back the response it gets over HTTP.

```bash
cd typescript
pnpm tsx ./m5/homework/call_agent_api.ts
```

Stuck, or want to see the finished shape before you start? `typescript/m5/homework_filled/` is a reference copy with every TODO filled in (a storm-chaser persona reporting on extreme weather records, queried both via Studio and via `call_agent_api.ts`); run it the same way (`cd typescript/m5/homework_filled && pnpm exec langgraphjs dev`, then `pnpm tsx ./m5/homework_filled/call_agent_api.ts` from `typescript/`) to see what "done" looks like end to end.

Try to avoid peeking unless you're really stuck, you'll get more out of this by working through the TODOs yourself first.

</div>

</div>

<script>
(function () {
  var tabs = document.querySelectorAll('.lt-tab');
  function show(p) {
    tabs.forEach(function (t) {
      var on = t.getAttribute('data-p') === p;
      t.classList.toggle('active', on);
      t.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    document.querySelectorAll('.lt-panel').forEach(function (panel) {
      panel.classList.toggle('active', panel.id === 'p-' + p);
    });
  }
  tabs.forEach(function (t) {
    t.addEventListener('click', function () { show(t.getAttribute('data-p')); });
  });
})();
</script>
