[For translation, open lesson in new tab and use Chrome translate](https://langchain-ai.github.io/lca-lessons/deepagents/m1/m1.3-models.html)

<style>@import url('../shared/sd-components.css');</style>
<script src="../shared/sd-components.js"></script>

<div class="page-lang-switch" data-page-lang-switch>
  <button class="page-lang-btn" data-page-lang="python" aria-selected="false">Python</button>
  <button class="page-lang-btn" data-page-lang="typescript" aria-selected="false">TypeScript</button>
</div>

# Models

<style>
.lt-bar {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin: 28px 0 0;
  border-bottom: 2px solid #CCE9FF;
}
.lt-group { display: flex; gap: 3px; }
.lt-models { --c: #0E9F6E; }
.lt-wrap   { --c: #B45309; }
.lt-tab {
  font: 500 14px 'IBM Plex Mono', monospace;
  padding: 9px 14px;
  border: none;
  background: transparent;
  color: #40668D;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  border-radius: 6px 6px 0 0;
  transition: background .15s, color .15s, border-color .15s;
  white-space: nowrap;
}
.lt-tab:hover { background: #F2FAFF; color: #030710; }
.lt-tab.active {
  color: var(--c);
  border-bottom-color: var(--c);
  background: #fff;
}
.lt-panel { display: none; padding-top: 24px; }
.lt-panel.active { display: block; }
@media (max-width: 600px) {
  .lt-bar { flex-wrap: nowrap; overflow-x: auto; gap: 12px; }
  .lt-tab { padding: 8px 10px; font-size: 13px; }
}
</style>

<div class="lt-bar" role="tablist" aria-label="Lesson sections">
  <div class="lt-group lt-models">
    <button class="lt-tab active" data-p="models" role="tab" aria-selected="true">Models</button>
  </div>
  <div class="lt-group lt-wrap">
    <button class="lt-tab" data-p="lab1" role="tab" aria-selected="false">Lab</button>
  </div>
</div>

<div class="lt-panel active" id="p-models" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

## Models: Use Any Model Provider

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/jpAgDzQoj7Z" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

<div style="margin:1.5rem 0;padding:1.25rem 1.5rem;border-radius:12px;border:1px solid #99d3ff;background-color:#e5f4ff;color:#1e4d7a;">
<strong>What is a model provider?</strong>
<p style="margin:0.5rem 0 0;">A model provider is a company or platform that hosts AI models and lets you call them through an API (e.g., OpenAI, Anthropic, Google). <code>langchain</code> gives you one consistent way to call those models: install the provider package, pick a model name, and swap models without rewriting your agent code.</p>
<p style="margin:0.75rem 0 0;"><code>langchain</code> supports 50+ chat model providers. See the full <a href="https://docs.langchain.com/oss/python/integrations/chat">chat model integrations</a> list for what's available.</p>
</div>

<div style="margin:1.5rem 0;padding:1.25rem 1.5rem;border-radius:12px;border:1px solid #99d3ff;background-color:#e5f4ff;color:#1e4d7a;display:flex;gap:1.25rem;align-items:flex-start;">
<img src="images/langchain-symbol.png" alt="LangChain" style="width:44px;height:44px;flex-shrink:0;margin-top:2px;object-fit:contain;" />
<div>
<p style="margin:0 0 0.5rem;"><code>langchain</code>'s model-agnostic design means you can swap in whatever fits your needs:</p>
<ul style="margin:0;padding-left:1.2rem;">
<li>a cheaper model to cut costs</li>
<li>an open-source model for privacy</li>
<li>a more capable one for complex reasoning</li>
</ul>
</div>
</div>

### Model Categories

<table style="width:100%;border-collapse:collapse;font-size:0.88em;margin:1.25rem 0 1.75rem;">
<thead>
<tr style="background-color:#d0eaff;color:#1e4d7a;text-align:left;">
<th style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Category</th>
<th style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Best for</th>
<th style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Examples</th>
</tr>
</thead>
<tbody>
<tr style="background-color:#e5f4ff;color:#1e4d7a;">
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Proprietary &amp; Conversational</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;">Fluid conversation, instruction-following, and general-purpose tasks</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;color:#40668D;">Claude Haiku/Sonnet, GPT-4.1, Gemini 2.0 Flash</td>
</tr>
<tr style="background-color:#f0f8ff;color:#1e4d7a;">
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Advanced Reasoning</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;">Multi-step problem-solving, math, code, and logic-heavy tasks</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;color:#40668D;">Claude Opus 4, o3, Gemini 2.5 Pro</td>
</tr>
<tr style="background-color:#e5f4ff;color:#1e4d7a;">
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Open-Source &amp; Open-Weight</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;">Local or private deployment; lower cost and full control over the model</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;color:#40668D;">Llama 3.3, Mistral, Qwen</td>
</tr>
</tbody>
</table>

<div style="margin:1.5rem 0;padding:0.75rem 1rem 0.75rem 1.25rem;border-left:4px solid #99d3ff;background-color:#e5f4ff;color:#1e4d7a;">Most models in all three categories also accept image inputs (and some accept audio). Multimodal support works through the same <code>init_chat_model</code> interface.</div>

The table above covers the main categories used in this course. For the full directory of 50+ supported chat model providers (OpenAI, Anthropic, Google, Groq, Ollama, Mistral, and many more), see the [LangChain chat model integrations](https://docs.langchain.com/oss/python/integrations/chat).

Every lab gets its model from a single shared file, **`python/models.py`**. It exports a `model` object built with `init_chat_model(...)`, and the lab scripts simply do `from models import model`.

To use a different model or model provider, edit `models.py` (comment out the active line and uncomment the desired model):

```python
# python/models.py
model = init_chat_model("anthropic:claude-haiku-4-5")   # workshop default, fast & cheap
# model = init_chat_model("ollama:qwen2.5:7b")
# model = init_chat_model("openai:gpt-4.1-mini")
# model = init_chat_model("openai:gpt-4.1")
```

If you set up Anthropic or OpenAI in the setup lesson, those work out of the box. If you want to use Azure, Bedrock, or Google, those require an extra install (`uv sync --extra azure|bedrock|google`) and additional env vars; see the notes at the top of `models.py`.

Head to **Lab 1** to run this yourself and see how the output changes across models. Scroll back to the top of this lesson and choose the **Lab** tab.

---

## Recap

- `init_chat_model("provider:model")` targets any `langchain`-supported chat model with a single string; swap the prefix to switch model providers, swap the suffix to switch models
- All labs share one model object from `python/models.py`; changing that one line is the only code change needed to run a different model
- Three categories in this course: Proprietary & Conversational, Advanced Reasoning, and Open-Source/Open-Weight; 50+ chat model providers are supported in total
- Swapping the model changes nothing else in the agent; the system prompt, tools, and behavior instructions stay identical

---

## Next

Complete the **Lab** above, then continue to the next lesson on the **system prompt**: how a single instruction string steers the agent's persona and domain without touching anything else.

---

## References

**Documentation:**

- [Deep Agents overview](https://docs.langchain.com/oss/python/deepagents/overview)
- [Models (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/models)
- [Customization & prompt assembly (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/customization#prompt-assembly)
- [Context engineering (Deep Agents)](https://docs.langchain.com/oss/python/deepagents/context-engineering)
- [deepagents README (GitHub)](https://github.com/langchain-ai/deepagents)
- [Built-in harness profiles (GitHub)](https://github.com/langchain-ai/deepagents/tree/main/libs/deepagents/deepagents/profiles/harness)
- [Chat model integrations (LangChain)](https://docs.langchain.com/oss/python/integrations/chat)

</div>

<div data-lang="typescript" markdown="1">

## Models: Use Any Model Provider

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lesson Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/jpAgDzQoj7Z" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

<div style="margin:1.5rem 0;padding:1.25rem 1.5rem;border-radius:12px;border:1px solid #99d3ff;background-color:#e5f4ff;color:#1e4d7a;">
<strong>What is a model provider?</strong>
<p style="margin:0.5rem 0 0;">A model provider is a company or platform that hosts AI models and lets you call them through an API (e.g., OpenAI, Anthropic, Google). <code>langchain</code> gives you one consistent way to call those models: install the provider package, pick a model name, and swap models without rewriting your agent code.</p>
<p style="margin:0.75rem 0 0;"><code>langchain</code> supports 50+ chat model providers. See the full <a href="https://docs.langchain.com/oss/javascript/integrations/chat">chat model integrations</a> list for what's available.</p>
</div>

<div style="margin:1.5rem 0;padding:1.25rem 1.5rem;border-radius:12px;border:1px solid #99d3ff;background-color:#e5f4ff;color:#1e4d7a;display:flex;gap:1.25rem;align-items:flex-start;">
<img src="images/langchain-symbol.png" alt="LangChain" style="width:44px;height:44px;flex-shrink:0;margin-top:2px;object-fit:contain;" />
<div>
<p style="margin:0 0 0.5rem;"><code>langchain</code>'s model-agnostic design means you can swap in whatever fits your needs:</p>
<ul style="margin:0;padding-left:1.2rem;">
<li>a cheaper model to cut costs</li>
<li>an open-source model for privacy</li>
<li>a more capable one for complex reasoning</li>
</ul>
</div>
</div>

### Model Categories

<table style="width:100%;border-collapse:collapse;font-size:0.88em;margin:1.25rem 0 1.75rem;">
<thead>
<tr style="background-color:#d0eaff;color:#1e4d7a;text-align:left;">
<th style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Category</th>
<th style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Best for</th>
<th style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Examples</th>
</tr>
</thead>
<tbody>
<tr style="background-color:#e5f4ff;color:#1e4d7a;">
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Proprietary &amp; Conversational</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;">Fluid conversation, instruction-following, and general-purpose tasks</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;color:#40668D;">Claude Haiku/Sonnet, GPT-4.1, Gemini 2.0 Flash</td>
</tr>
<tr style="background-color:#f0f8ff;color:#1e4d7a;">
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Advanced Reasoning</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;">Multi-step problem-solving, math, code, and logic-heavy tasks</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;color:#40668D;">Claude Opus 4, o3, Gemini 2.5 Pro</td>
</tr>
<tr style="background-color:#e5f4ff;color:#1e4d7a;">
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;font-weight:600;">Open-Source &amp; Open-Weight</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;">Local or private deployment; lower cost and full control over the model</td>
<td style="padding:0.6rem 0.9rem;border:1px solid #99d3ff;color:#40668D;">Llama 3.3, Mistral, Qwen</td>
</tr>
</tbody>
</table>

<div style="margin:1.5rem 0;padding:0.75rem 1rem 0.75rem 1.25rem;border-left:4px solid #99d3ff;background-color:#e5f4ff;color:#1e4d7a;">Most models in all three categories also accept image inputs (and some accept audio). Multimodal support works through the same <code>initChatModel</code> interface.</div>

The table above covers the main categories used in this course. For the full directory of 50+ supported chat model providers (OpenAI, Anthropic, Google, Groq, Ollama, Mistral, and many more), see the [LangChain chat model integrations](https://docs.langchain.com/oss/javascript/integrations/chat).

Every lab gets its model from a single shared file, **`typescript/models.ts`**. It exports a `model` object built with `initChatModel(...)`, and the lab scripts simply do `import { model } from "../models.js"`.

To use a different model or model provider, edit `models.ts` (comment out the active line and uncomment the desired model):

```typescript
// typescript/models.ts
export const model = await initChatModel("anthropic:claude-haiku-4-5");   // workshop default, fast & cheap
// export const model = await initChatModel("ollama:qwen2.5:7b");
// export const model = await initChatModel("openai:gpt-4.1-mini");
// export const model = await initChatModel("openai:gpt-4.1");
```

If you set up Anthropic or OpenAI in the setup lesson, those work out of the box. If you want to use Bedrock or Google, those require an extra install (`@langchain/aws` and `@langchain/google-genai`) and additional env vars; see the notes at the top of `models.ts`.

Head to **Lab 1** to run this yourself and see how the output changes across models. Scroll back to the top of this lesson and choose the **Lab** tab.

---

## Recap

- `initChatModel("provider:model")` targets any `langchain`-supported chat model with a single string; swap the prefix to switch model providers, swap the suffix to switch models
- All labs share one model object from `typescript/models.ts`; changing that one line is the only code change needed to run a different model
- Three categories in this course: Proprietary & Conversational, Advanced Reasoning, and Open-Source/Open-Weight; 50+ chat model providers are supported in total
- Swapping the model changes nothing else in the agent; the system prompt, tools, and behavior instructions stay identical

---

## Next

Complete the **Lab** above, then continue to the next lesson on the **system prompt**: how a single instruction string steers the agent's persona and domain without touching anything else.

---

## References

**Documentation:**

- [Deep Agents overview](https://docs.langchain.com/oss/javascript/deepagents/overview)
- [Models (Deep Agents)](https://docs.langchain.com/oss/javascript/deepagents/models)
- [Customization & prompt assembly (Deep Agents)](https://docs.langchain.com/oss/javascript/deepagents/customization#prompt-assembly)
- [Context engineering (Deep Agents)](https://docs.langchain.com/oss/javascript/deepagents/context-engineering)
- [deepagents README (GitHub)](https://github.com/langchain-ai/deepagents)
- [Built-in harness profiles (GitHub)](https://github.com/langchain-ai/deepagents/tree/main/libs/deepagents/deepagents/profiles/harness)
- [Chat model integrations (LangChain)](https://docs.langchain.com/oss/javascript/integrations/chat)

</div>

</div>

<div class="lt-panel" id="p-lab1" markdown="1" role="tabpanel">

<div data-lang="python" markdown="1">

## Models

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lab Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/MKueYS2eqAV" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

First, run your scratch agent from the previous lesson with the default model:

```bash
cd python
uv run ./m1/m1.2_scratch_agent.py
```

Next, open `python/models.py` and switch the model: comment out the active Haiku line and uncomment another model line you want to try.

After you change `models.py`, run the same script again and compare the output:

```bash
cd python
uv run ./m1/m1.2_scratch_agent.py
```

This also works with open-source models; no code changes required, just a different model line in `models.py`.

<div style="margin:1.5rem 0;padding:1.25rem 1.5rem;border-radius:12px;border:1px solid #b3e0b3;background-color:#f0faf0;color:#1a4d1a;">
<strong>Why use an open-source model?</strong>
<ul style="margin:0.75rem 0 0;padding-left:1.2rem;">
<li><strong>Privacy:</strong> data never leaves your machine when running locally (using a local runtime such as Ollama); useful for sensitive or proprietary content</li>
<li><strong>Cost:</strong> no per-token charges when self-hosted; many hosted options are free or significantly cheaper than proprietary APIs</li>
<li><strong>No vendor lock-in:</strong> swap between different providers and models (e.g. Llama, Mistral, Qwen) with a single line change</li>
<li><strong>Customization:</strong> open weights mean you can fine-tune the model on your own data</li>
</ul>
</div>

<div style="margin:1.5rem 0;padding:0.75rem 1rem 0.75rem 1.25rem;border-left:4px solid #4ade80;background-color:#f0faf0;color:#1a4d1a;">
<strong>Try an open-source model via OpenRouter</strong>
<p style="margin:0.5rem 0 0;">OpenRouter gives you access to free hosted open-source models with no extra install. If you added your <code>OPENROUTER_API_KEY</code> to <code>.env</code> during setup, just uncomment the OpenRouter lines in <code>models.py</code> and run the lab again. If you haven't added it yet, head back to the <a href="../m0/m0.1-setup-python.html">setup lesson</a> to add it to your <code>.env</code> file.</p>
<p style="margin:0.5rem 0 0;">Model used in this course: <a href="https://openrouter.ai/nvidia/nemotron-3-ultra-550b-a55b:free">nvidia/nemotron-3-ultra-550b-a55b:free on OpenRouter</a></p>
<p style="margin:0.75rem 0 0;font-size:0.9em;color:#3a5a3a;"><strong>Note:</strong> Free models on hosted platforms typically mean you are consenting to the provider using your prompts and completions as training data. Avoid sending sensitive or proprietary information through free-tier endpoints.</p>
</div>

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Open-Source (Nvidia) Setup</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/O1Vjox9ay96" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>


Here are two real responses to "What is an LLM?", one from each model:

<div style="display:flex;gap:20px;align-items:flex-start;margin:1.5rem 0;">

<details style="flex:1;border:1px solid #e5e7eb;border-radius:8px;padding:0.75rem 1rem;">
<summary style="cursor:pointer;font-weight:600;font-family:'Monaco','Courier New',monospace;font-size:0.875em;list-style:none;">claude-haiku-4-5</summary>
<div style="margin-top:1rem;">
<p>An LLM (Large Language Model) is a type of artificial intelligence system trained on vast amounts of text data to predict and generate human language.</p>
<p><strong>Key characteristics:</strong></p>
<ul>
<li><strong>Scale</strong>: Contains billions to hundreds of billions of parameters (learnable weights), which is why they're "large"</li>
<li><strong>Training</strong>: Learns patterns from massive text datasets using transformer-based neural networks</li>
<li><strong>Capability</strong>: Can perform tasks like answering questions, writing, summarizing, translating, coding, and reasoning</li>
<li><strong>Generative</strong>: Produces text one word (or "token") at a time, based on patterns learned during training</li>
<li><strong>Context-aware</strong>: Maintains understanding of conversation history and context within a session</li>
</ul>
<p><strong>Common examples</strong>: ChatGPT, Claude, Gemini, Llama, and Mistral are all LLMs.</p>
<p>LLMs don't truly "understand" in the human sense; they're sophisticated pattern-matching systems that are remarkably effective at tasks requiring language comprehension and generation.</p>
</div>
</details>

<details style="flex:1;border:1px solid #e5e7eb;border-radius:8px;padding:0.75rem 1rem;">
<summary style="cursor:pointer;font-weight:600;font-family:'Monaco','Courier New',monospace;font-size:0.875em;list-style:none;">nvidia/nemotron-3-ultra-550b-a55b</summary>
<div style="margin-top:1rem;">
<p>An <strong>LLM (Large Language Model)</strong> is a type of artificial intelligence model trained on vast amounts of text data to understand and generate human-like language.</p>
<p><strong>Key characteristics:</strong></p>
<ul>
<li><strong>Scale</strong>: Billions to trillions of parameters (e.g., GPT-4, Claude, Llama)</li>
<li><strong>Training</strong>: Self-supervised learning on diverse text corpora (books, websites, code, etc.)</li>
<li><strong>Architecture</strong>: Typically based on the Transformer architecture with attention mechanisms</li>
<li><strong>Capabilities</strong>: Text generation, summarization, translation, coding, reasoning, question answering</li>
</ul>
<p><strong>How it works:</strong> LLMs predict the next token (word/character) in a sequence based on context. Through massive training, they learn patterns, facts, reasoning, and linguistic structures.</p>
<p><strong>Limitations:</strong> They can hallucinate (generate false information), lack true understanding, have knowledge cutoffs, and can be biased by training data.</p>
</div>
</details>

</div>

---

### Ollama: run a model locally

**[NO API KEY NEEDED]**

Ollama is a tool for downloading and running open-source models directly on your machine. No API key, no data sent to a third party: the model runs entirely on your hardware.

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Open-Source (Ollama) Setup</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/5t0FLKmmD7h" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>


**Step 1: Install Ollama** from [ollama.com](https://ollama.com)

**Step 2: Pull a model:**

```bash
ollama pull qwen2.5:7b
```

**Step 3: Update `python/models.py`.** Comment out the active line and uncomment the Ollama line:

```python
# model = init_chat_model("anthropic:claude-haiku-4-5")
model = init_chat_model("ollama:qwen2.5:7b")
```

**Step 4: Run the lab as normal.** No other code changes needed.

The agent code and system prompt are identical. Only the model line in `models.py` is different.

> **Local only.** Ollama also offers hosted cloud models at ollama.com, which do require an API key. These steps are for running a model locally on your own machine.

<div style="margin:1.5rem 0;padding:1.25rem 1.5rem;border-radius:12px;border:1px solid #fcd34d;background-color:#fefce8;color:#713f12;">
<strong>Heads up: small or older local models can struggle with deep agents</strong>
<p style="margin:0.75rem 0 0;">Deep agent workflows lean hard on reliable tool calling and system-prompt adherence. Tiny local models are best suited to lightweight tasks like summarization, not agentic work like this.</p>
<ul style="margin:0.75rem 0 0;padding-left:1.2rem;">
<li>Consumer hardware generally can't run frontier-capable open models locally, which is part of why Ollama itself sells <a href="https://ollama.com/cloud">hosted cloud models</a> for the larger ones.</li>
<li>Open-weight models have improved dramatically in recent releases, so if yours ignores your system prompt, skips tool calls, or loops, that's likely a model-capability limit (not a bug in your setup).</li>
<li>Hardware permitting, pull a newer, larger model instead (check what your machine can handle before pulling one):
<ul style="margin:0.5rem 0 0;padding-left:1.2rem;">
<li>7B models: ~8GB RAM</li>
<li>14B models: ~16GB RAM</li>
<li>32B models: ~24GB+ RAM</li>
<li>70B-class models (e.g. Llama 3.3 70B): ~64GB RAM</li>
</ul>
</li>
<li>Otherwise, use the <strong>OpenRouter</strong> option above for a larger open-weight model without the local hardware requirement.</li>
</ul>
</div>

</div>

<div data-lang="typescript" markdown="1">

## Models

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Lab Video Walkthrough</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/MKueYS2eqAV" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>

First, run your scratch agent from the previous lesson with the default model:

```bash
cd typescript
pnpm tsx ./m1/m1.2_scratch_agent.ts
```

Next, open `typescript/models.ts` and switch the model: comment out the active Haiku line and uncomment another model line you want to try.

After you change `models.ts`, run the same script again and compare the output:

```bash
cd typescript
pnpm tsx ./m1/m1.2_scratch_agent.ts
```

This also works with open-source models; no code changes required, just a different model line in `models.ts`.

<div style="margin:1.5rem 0;padding:1.25rem 1.5rem;border-radius:12px;border:1px solid #b3e0b3;background-color:#f0faf0;color:#1a4d1a;">
<strong>Why use an open-source model?</strong>
<ul style="margin:0.75rem 0 0;padding-left:1.2rem;">
<li><strong>Privacy:</strong> data never leaves your machine when running locally (using a local runtime such as Ollama); useful for sensitive or proprietary content</li>
<li><strong>Cost:</strong> no per-token charges when self-hosted; many hosted options are free or significantly cheaper than proprietary APIs</li>
<li><strong>No vendor lock-in:</strong> swap between different providers and models (e.g. Llama, Mistral, Qwen) with a single line change</li>
<li><strong>Customization:</strong> open weights mean you can fine-tune the model on your own data</li>
</ul>
</div>

<div style="margin:1.5rem 0;padding:0.75rem 1rem 0.75rem 1.25rem;border-left:4px solid #4ade80;background-color:#f0faf0;color:#1a4d1a;">
<strong>Try an open-source model via OpenRouter</strong>
<p style="margin:0.5rem 0 0;">OpenRouter gives you access to free hosted open-source models with no extra install. If you added your <code>OPENROUTER_API_KEY</code> to <code>.env</code> during setup, just uncomment the OpenRouter lines in <code>models.ts</code> and run the lab again. If you haven't added it yet, head back to the <a href="../m0/m0.1-setup-typescript.html">setup lesson</a> to add it to your <code>.env</code> file.</p>
<p style="margin:0.5rem 0 0;">Model used in this course: <a href="https://openrouter.ai/nvidia/nemotron-3-ultra-550b-a55b:free">nvidia/nemotron-3-ultra-550b-a55b:free on OpenRouter</a></p>
<p style="margin:0.75rem 0 0;font-size:0.9em;color:#3a5a3a;"><strong>Note:</strong> Free models on hosted platforms typically mean you are consenting to the provider using your prompts and completions as training data. Avoid sending sensitive or proprietary information through free-tier endpoints.</p>
</div>


<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Open-Source (Nvidia) Setup</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/O1Vjox9ay96" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>


Here are two real responses to "What is an LLM?", one from each model:

<div style="display:flex;gap:20px;align-items:flex-start;margin:1.5rem 0;">

<details style="flex:1;border:1px solid #e5e7eb;border-radius:8px;padding:0.75rem 1rem;">
<summary style="cursor:pointer;font-weight:600;font-family:'Monaco','Courier New',monospace;font-size:0.875em;list-style:none;">claude-haiku-4-5</summary>
<div style="margin-top:1rem;">
<p>An LLM (Large Language Model) is a type of artificial intelligence system trained on vast amounts of text data to predict and generate human language.</p>
<p><strong>Key characteristics:</strong></p>
<ul>
<li><strong>Scale</strong>: Contains billions to hundreds of billions of parameters (learnable weights), which is why they're "large"</li>
<li><strong>Training</strong>: Learns patterns from massive text datasets using transformer-based neural networks</li>
<li><strong>Capability</strong>: Can perform tasks like answering questions, writing, summarizing, translating, coding, and reasoning</li>
<li><strong>Generative</strong>: Produces text one word (or "token") at a time, based on patterns learned during training</li>
<li><strong>Context-aware</strong>: Maintains understanding of conversation history and context within a session</li>
</ul>
<p><strong>Common examples</strong>: ChatGPT, Claude, Gemini, Llama, and Mistral are all LLMs.</p>
<p>LLMs don't truly "understand" in the human sense; they're sophisticated pattern-matching systems that are remarkably effective at tasks requiring language comprehension and generation.</p>
</div>
</details>

<details style="flex:1;border:1px solid #e5e7eb;border-radius:8px;padding:0.75rem 1rem;">
<summary style="cursor:pointer;font-weight:600;font-family:'Monaco','Courier New',monospace;font-size:0.875em;list-style:none;">nvidia/nemotron-3-ultra-550b-a55b</summary>
<div style="margin-top:1rem;">
<p>An <strong>LLM (Large Language Model)</strong> is a type of artificial intelligence model trained on vast amounts of text data to understand and generate human-like language.</p>
<p><strong>Key characteristics:</strong></p>
<ul>
<li><strong>Scale</strong>: Billions to trillions of parameters (e.g., GPT-4, Claude, Llama)</li>
<li><strong>Training</strong>: Self-supervised learning on diverse text corpora (books, websites, code, etc.)</li>
<li><strong>Architecture</strong>: Typically based on the Transformer architecture with attention mechanisms</li>
<li><strong>Capabilities</strong>: Text generation, summarization, translation, coding, reasoning, question answering</li>
</ul>
<p><strong>How it works:</strong> LLMs predict the next token (word/character) in a sequence based on context. Through massive training, they learn patterns, facts, reasoning, and linguistic structures.</p>
<p><strong>Limitations:</strong> They can hallucinate (generate false information), lack true understanding, have knowledge cutoffs, and can be biased by training data.</p>
</div>
</details>

</div>

---

### Ollama: run a model locally

**[NO API KEY NEEDED]**

Ollama is a tool for downloading and running open-source models directly on your machine. No API key, no data sent to a third party: the model runs entirely on your hardware.

<details style="border:2.5px solid #000;border-radius:6px;background:#fff;margin:1rem 0;"><summary style="padding:10px 16px;cursor:pointer;font-weight:500;font-family:'IBM Plex Mono',monospace;font-size:14px;">Open-Source (Ollama) Setup</summary><div style="padding:12px 16px 16px;"><div class="video-container" style="max-width:750px;"><div class="video-wrapper"><iframe src="https://share.descript.com/embed/5t0FLKmmD7h" frameborder="0" allow="autoplay; fullscreen; encrypted-media; picture-in-picture" allowfullscreen></iframe></div></div></div></details>


**Step 1: Install Ollama** from [ollama.com](https://ollama.com)

**Step 2: Pull a model:**

```bash
ollama pull qwen2.5:7b
```

**Step 3: Update `typescript/models.ts`.** Comment out the active line and uncomment the Ollama line:

```typescript
// export const model = await initChatModel("anthropic:claude-haiku-4-5");
export const model = await initChatModel("ollama:qwen2.5:7b");
```

**Step 4: Run the lab as normal.** No other code changes needed.

The agent code and system prompt are identical. Only the model line in `models.ts` is different.

> **Local only.** Ollama also offers hosted cloud models at ollama.com, which do require an API key. These steps are for running a model locally on your own machine.

<div style="margin:1.5rem 0;padding:1.25rem 1.5rem;border-radius:12px;border:1px solid #fcd34d;background-color:#fefce8;color:#713f12;">
<strong>Heads up: small or older local models can struggle with deep agents</strong>
<p style="margin:0.75rem 0 0;">Deep agent workflows lean hard on reliable tool calling and system-prompt adherence. Tiny local models are best suited to lightweight tasks like summarization, not agentic work like this.</p>
<ul style="margin:0.75rem 0 0;padding-left:1.2rem;">
<li>Consumer hardware generally can't run frontier-capable open models locally, which is part of why Ollama itself sells <a href="https://ollama.com/cloud">hosted cloud models</a> for the larger ones.</li>
<li>Open-weight models have improved dramatically in recent releases, so if yours ignores your system prompt, skips tool calls, or loops, that's likely a model-capability limit (not a bug in your setup).</li>
<li>Hardware permitting, pull a newer, larger model instead (check what your machine can handle before pulling one):
<ul style="margin:0.5rem 0 0;padding-left:1.2rem;">
<li>7B models: ~8GB RAM</li>
<li>14B models: ~16GB RAM</li>
<li>32B models: ~24GB+ RAM</li>
<li>70B-class models (e.g. Llama 3.3 70B): ~64GB RAM</li>
</ul>
</li>
<li>Otherwise, use the <strong>OpenRouter</strong> option above for a larger open-weight model without the local hardware requirement.</li>
</ul>
</div>

</div>

</div>

<script>
(function () {
  var tabs = document.querySelectorAll('.lt-tab');
  function show(p) {
    tabs.forEach(function (t) {
      var on = t.getAttribute('data-p') === p;
      t.classList.toggle('active', on);
      t.setAttribute('aria-selected', on ? 'true' : 'false');
    });
    document.querySelectorAll('.lt-panel').forEach(function (panel) {
      panel.classList.toggle('active', panel.id === 'p-' + p);
    });
  }
  tabs.forEach(function (t) {
    t.addEventListener('click', function () { show(t.getAttribute('data-p')); });
  });
})();
</script>
